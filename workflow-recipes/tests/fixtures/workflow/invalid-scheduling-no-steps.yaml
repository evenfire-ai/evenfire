apiVersion: clerum.io/v1alpha1
kind: WorkflowRecipe
metadata:
  name: invalid-sched-no-steps
  namespace: sandbox-recipes
spec:
  workloads:
    - id: app
      type: deployment
      image: nginx:1.30.1-alpine
  scheduling:
    cron: '0 9 * * *'
