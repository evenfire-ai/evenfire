{
  "name": "clerum-webhook-proxy",
  "version": "0.1.0",
  "description": "Cluster-shared webhook ingress: registry-validated, dumb router to per-recipe webhook gateways.",
  "main": "dist/main.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "kubernetes",
    "webhook",
    "ingress",
    "clerum"
  ],
  "license": "MPL-2.0",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^7.0.2",
    "vitest": "^4.0.18"
  }
}
