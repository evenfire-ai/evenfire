# `@clerum/frontend-table-system`

The canonical structural table system shared by Control UI and Profile UI.

Import composable React primitives from `@clerum/frontend-table-system` and import the single global
stylesheet from `@clerum/frontend-table-system/styles.css` in each application's root layout. Apps
retain ownership of data loading, routes, permissions, domain cells, and mutation behavior.
