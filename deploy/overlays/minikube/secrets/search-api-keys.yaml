apiVersion: v1
kind: Secret
metadata:
  name: search-api-keys
  namespace: mcp-server
type: Opaque
stringData:
  # Brave Search API key — get one free at https://api.search.brave.com/
  # Free tier: 2,000 queries/month
  SEARCH_API_KEY: "${BRAVE_API_KEY:-brave-search-placeholder-key}"
