# Minikube: Grok subscriptions on for workflow recipes.
#
# Base ships this switch off, like every subscription switch; each environment's
# overlay turns it on, so minikube runs both subscription brokers. The switch is a
# literal env entry on the Deployment (not a ConfigMap key), so it is patched
# here rather than in configmaps/control-api-config.yaml.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-recipes
  namespace: control-plane
spec:
  template:
    spec:
      containers:
        - name: workflow-recipes
          env:
            - name: WRC_GROK_SUBSCRIPTION_ENABLED
              value: "true"
