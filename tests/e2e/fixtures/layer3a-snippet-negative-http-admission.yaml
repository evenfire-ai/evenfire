apiVersion: clerum.io/v1alpha1
kind: WorkflowRecipe
metadata:
  name: e2e-layer3a-snippet-negative-http-admission
  namespace: sandbox-recipes
spec:
  triggers:
    onDemand: {}
  runtimeEgress:
    http:
      allowedHosts:
        - api.ipify.org
  steps:
    - id: call-public-api
      run:
        type: snippet
        language: typescript
        code: |
          return await sdk.http.fetchJson("https://jsonplaceholder.typicode.com/todos/1")
        capabilities:
          http:
            allowedHosts:
              - jsonplaceholder.typicode.com
