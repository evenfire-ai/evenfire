# Copilot instructions

Frontend guidance is path-scoped through the applicable files under
`.github/instructions/`. Those adapters route to provider-neutral canonical
documents and must not be replaced with repository-wide frontend semantics.
